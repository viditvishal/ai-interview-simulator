// ── Supabase Client ──────────────────────────────────────────────────────────
// Placeholder — will be configured when Supabase project is created.
// For now, the app uses localStorage persistence via Zustand.
//
// To enable Supabase:
// 1. Create a project at https://supabase.com
// 2. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY to .env
// 3. Uncomment the client below

// import { createClient } from '@supabase/supabase-js'
//
// const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
// const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY
//
// export const supabase = createClient(supabaseUrl, supabaseAnonKey)

export const supabaseEnabled = false
